{
  "name": "agent-schedule-executor",
  "version": "1.0.0",
  "description": "Lambda function to execute scheduled agent tasks via EventBridge Scheduler",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/node": "^18.19.118",
    "typescript": "^5.0.0"
  }
}
