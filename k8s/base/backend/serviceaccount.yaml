apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentx-backend-sa
  namespace: agentx
  annotations:
    # Replace with actual IAM role ARN created via eksctl/IRSA
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/agentx-backend-irsa-role
  labels:
    app.kubernetes.io/name: agentx-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: agentx
